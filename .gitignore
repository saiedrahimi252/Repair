# تنظیمات محلی و رمزها - هرگز نباید وارد گیت شود
.env

# فایل‌های کش پایتون
__pycache__/
*.pyc
*.pyo

# محیط مجازی پایتون (در صورت استفاده)
venv/
.venv/
env/

# فایل‌های موقت ویندوز/ویرایشگر
*.log
.DS_Store
Thumbs.db

# دیتابیس محلی قدیمی (اگر باقی مانده باشد)
data/*.db

# --------------------------------------------------------------------
# این‌ها اطلاعات واقعی و داخلی شرکت دارند (اسامی، شرح نقص‌ها، کدهای
# دستگاه‌های واقعی و ...) و نباید در یک ریپوزیتوری عمومی قرار بگیرند.
# این فایل‌ها همچنان روی کامپیوتر شما می‌مانند و برنامه به آن‌ها دسترسی
# دارد؛ فقط وارد گیت‌هاب نمی‌شوند.
repair_sqlserver.sql
key_equipment_template.xlsx
