# Project Context — Repair

## شناسنامه
- Repository: `saiedrahimi252/Repair`
- Branch اصلی فعلی: `main`
- نوع پروژه: Python Web Application
- دیتابیس: Microsoft SQL Server
- دسترسی دیتابیس از Python: `pyodbc`
- فایل اجرای production: `run_production.py`

## ساختار مهم
فایل‌های اصلی فعلی Repository شامل:
- `app.py` — منطق اصلی برنامه و routeهای وب
- `auth.py` — احراز هویت
- `database.py` — لایه اتصال/سازگارسازی SQL Server
- `import_legacy_data.py` — وارد کردن داده‌های قدیمی
- `jalali.py` — توابع مرتبط با تاریخ جلالی
- `text_utils.py` — توابع کمکی متن
- `run_production.py` — اجرای production
- `repair_sqlserver.sql` — ساختار/داده SQL Server
- `requirements.txt` — وابستگی‌های Python
- `templates/` — قالب‌های HTML
- `.env.example` — نمونه تنظیمات اتصال

## معماری دیتابیس
`database.py` با `pyodbc` به SQL Server وصل می‌شود و بخشی از SQL به سبک SQLite موجود در برنامه را به T-SQL تبدیل می‌کند.

تنظیمات مهم محیطی:
- `MSSQL_SERVER`
- `MSSQL_DATABASE`
- `MSSQL_UID`
- `MSSQL_PWD`
- `MSSQL_DRIVER`
- `MSSQL_TRUSTED_CONNECTION`
- `MSSQL_ENCRYPT`
- `MSSQL_TRUST_SERVER_CERTIFICATE`

مقدار پیش‌فرض در کد برای Driver، `ODBC Driver 18 for SQL Server` است.

## نکته مهم
README فعلی می‌گوید برنامه از SQL Server استفاده می‌کند و اگر دیتابیس از صفر ساخته شود، برنامه می‌تواند جدول‌های لازم را در اولین اجرا ایجاد کند. همچنین `repair_sqlserver.sql` برای آماده‌سازی دیتابیس وجود دارد.

## وضعیت فعلی
این فایل یک مرجع پایه است. وضعیت واقعی کار باید همیشه در `HANDOFF.md` ثبت شود.


## وضعیت به‌روزشده پروژه — 2026-09-25
آخرین commitهای مهم:
- `0cb2c1b9299bbbf61fb48c71e816d7031a8d1ba7` — محدود کردن حذف درخواست به مدیر سیستم؛ فقط `admin` مجاز به حذف درخواست است.
- `7ddbe3f6fac9651876f641dbe760b16e05f643a3` — افزودن ستون‌های `review_locked_by` و `review_locked_at` به جدول `data`.
- `bcdd86491529a14b0bdfe55719b3e06c33e6d4dd` — قفل درخواست هنگام ورود به بررسی و تکمیل برای جلوگیری از بررسی همزمان یک درخواست توسط چند نیروی تعمیرات.
- `606274f50d8ac35c51003cdc33cb9292a807e349` — اصلاح پیش‌فرض‌های SQL Server برای استقرار Windows SQLEXPRESS.

### پروژه توسعه آتی: انتقال ماژول Excel تولید به نرم‌افزار
فایل Excel بررسی‌شده در این جلسه شامل شیت‌های زیر است:
`1`, `data`, `برنامه`, `حضور `, `ک روزانه`, `گ ضایعات`, `مرخصی بین`, `گزارش تولید`, `print`, `گزارش کارکرد`, `تشویقی `, `Sheet1`, `ضایعات`, `کنترل`, `تولید ایستگاه`, `تشویقی  نهائی تولید`, `رسید`, `تحویل به خط`, `تولید`, `ارسال`, `گزارش`, `گزا رش مدیریتی`, `تشویقی نهایی کنترل`, `توضیحات`, `OEE`.

این Excel باید به‌عنوان منبع نیازمندی/منطق کسب‌وکار بررسی شود، نه اینکه عیناً به صفحات متعدد وب تبدیل شود. هدف، ایجاد یک ماژول مستقل «تولید» در کنار ماژول تعمیرات فعلی و استفاده از همان SQL Server/Python است.

ماژول تولید باید در طراحی آینده حداقل حوزه‌های زیر را پوشش دهد:
- برنامه تولید
- حضور، مرخصی و کارکرد
- ثبت تولید
- تولید به تفکیک ایستگاه
- ثبت و گزارش ضایعات
- کنترل تولید/ضایعات
- رسید مواد
- تحویل مواد به خط
- تولید/مصرف
- ارسال محصول
- تشویقی تولید
- تشویقی کنترل
- گزارش‌های عملیاتی و مدیریتی
- OEE

قانون طراحی: فرمول‌ها و منطق Excel باید ابتدا استخراج و اعتبارسنجی شوند؛ فرمول‌های خراب یا ارجاع‌های `#REF!` نباید کورکورانه به Python/SQL منتقل شوند. ابتدا مدل داده و گردش کار طراحی شود، سپس پیاده‌سازی مرحله‌ای انجام شود.

در صورت نیاز به ارتباط با CMMS، توقف‌های تعمیراتی و مدت توقف دستگاه باید بتوانند در گزارش‌های تولید/OEE به‌صورت داده مرتبط استفاده شوند.

### اصل مهم برای AIهای بعدی
این Repository منبع اصلی handoff پروژه است. هر AI که به Repository دسترسی دارد باید قبل از تغییر، فایل‌های `AI_CONTEXT/` را بخواند و بعد از کار وضعیت واقعی، تست‌ها، commit و قدم بعدی را ثبت کند.
