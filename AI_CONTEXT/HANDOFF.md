# Handoff — وضعیت تحویل کار بین AIها

## تاریخ آخرین به‌روزرسانی
2026-09-25

## آخرین وضعیت
پروژه Repair روی GitHub فعال است و تغییرات اخیر بخش تعمیرات در Git ثبت شده‌اند. کار بعدی، طراحی انتقال سیستم تولید موجود در Excel به یک ماژول وب/SQL Server در همان پروژه است.

## آخرین کارهای انجام‌شده
1. محدود شدن حذف درخواست به نقش `admin`.
2. اضافه شدن قفل همزمانی برای درخواست‌های در حال بررسی.
3. اضافه شدن ستون‌های `review_locked_by` و `review_locked_at`.
4. بررسی اولیه فایل Excel تولید و شناسایی 25 شیت اصلی.
5. تصمیم معماری: Excel عیناً کپی نشود؛ منطق آن به‌صورت مدل داده، فرم‌ها و گزارش‌های وب پیاده شود.

## Commitهای اخیر
- `0cb2c1b9299bbbf61fb48c71e816d7031a8d1ba7`
- `7ddbe3f6fac9651876f641dbe760b16e05f643a3`
- `bcdd86491529a14b0bdfe55719b3e06c33e6d4dd`
- `606274f50d8ac35c51003cdc33cb9292a807e349`

## Excel مورد بررسی
فایل تولیدی شامل شیت‌های حضور، برنامه، تولید، تولید ایستگاه، ضایعات، کنترل، رسید، تحویل به خط، ارسال، تشویقی، گزارش مدیریتی و OEE و چند شیت داده/کمکی است. ابعاد شیت‌ها بررسی شده و در صورت نیاز باید در مرحله تحلیل جزئیات هر شیت، فرمول‌ها و وابستگی‌ها استخراج شوند.

## تست‌ها
- تغییرات اخیر GitHub ثبت شده‌اند.
- تست اجرایی کامل روی سرور واقعی برای تغییرات اخیر هنوز باید انجام شود.
- برای ماژول تولید هنوز هیچ جدول یا صفحه جدیدی به‌عنوان پیاده‌سازی نهایی ساخته نشده است.

## مشکلات/ریسک‌های فعلی
- منطق کامل Excel هنوز استخراج نشده است.
- بعضی فرمول‌های Excel دارای ارجاع خراب مانند `#REF!` هستند و نباید بدون تحلیل منتقل شوند.
- سطح دسترسی نقش‌های تولید هنوز نهایی نشده است.
- مدل دیتابیس تولید هنوز طراحی نشده است.

## قدم بعدی دقیق برای AI بعدی
1. Excel را شیت‌به‌شیت تحلیل کن: ستون‌ها، ورودی‌ها، فرمول‌ها، وابستگی به شیت‌های دیگر، خروجی و نقش کاربر.
2. موارد `#REF!` و فرمول‌های مشکوک را جداگانه فهرست کن.
3. بر اساس این تحلیل یک مدل داده پیشنهادی برای SQL Server و گردش کار پیشنهادی تهیه کن؛ هنوز بدون تغییر گسترده در کد.
4. طرح را با ساختار فعلی `database.py`، نقش‌های `auth.py` و دستگاه‌ها/تعمیرات موجود تطبیق بده.
5. پس از تثبیت طراحی، مرحله اول پیاده‌سازی را در GitHub انجام بده و همین فایل را دوباره به‌روزرسانی کن.

## قانون برای AI بعدی
قبل از هر تغییر، `AI_CONTEXT/AI_INSTRUCTIONS.md`، `PROJECT_CONTEXT.md`، `CURRENT_TASK.md`، `HANDOFF.md` و `CHANGELOG.md` را بخوان. بعد از کار، وضعیت واقعی و commit را ثبت کن.
