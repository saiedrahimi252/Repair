# Current Task

## هدف اصلی فعلی
پیاده‌سازی مرحله‌ای ماژول «تولید» در نرم‌افزار Repair بر اساس Excel تولیدی بررسی‌شده در 2026-09-25.

## مرحله فعلی
تثبیت طراحی و آماده‌سازی فاز 1. migration اجرایی هنوز اجرا نشده است.

## کارهای انجام‌شده
- ساختار 25 شیت Excel شناسایی شد.
- وابستگی‌های اصلی و جریان مواد استخراج شد.
- منطق اولیه OEE استخراج شد.
- خطاهای #REF! ثبت شدند.
- ERD منطقی مرحله اول ساخته شد.
- تطبیق اولیه ERD با database.py و CMMS انجام شد.
- برنامه پیاده‌سازی مرحله‌ای در AI_CONTEXT/PRODUCTION_IMPLEMENTATION_PLAN.md ثبت شد.

## کارهای باقی‌مانده فاز فعلی
- اعتبارسنجی معادل آحاد و time_weight.
- اعتبارسنجی زمان حضور مفید/کارکرد برنامه‌ای/کارکرد ساعت‌زن.
- تعریف رسمی انواع توقف و اثر آنها بر OEE.
- تعریف حد مجاز ضایعات.
- استخراج فرمول کامل تشویقی.
- تثبیت تاریخ کاری شیفت شب.
- نهایی‌کردن رابطه محصول، ایستگاه و ماشین.
- مشخص‌کردن نقش‌های واقعی تولید.

## فاز 1 بعد از تثبیت
Master Data و برنامه تولید:
production_products, production_suppliers, production_raw_materials, production_stations, production_machines, production_station_products, production_employees, production_shifts, production_plans, production_plan_items.

## محدودیت
تا قبل از تثبیت منطق و کلیدها، migration گسترده یا import انبوه انجام نشود. اتصال production_machines به dastgahjadid.cod در ابتدا منطقی/کنترلی باشد و FK فیزیکی ایجاد نشود.


## آخرین پیشرفت اجرایی — 2026-09-25
- صفحات Master Data برای ماشین‌های تولید، پرسنل تولید و شیفت‌ها اضافه شد.
- مسیرهای جدید: /production/machines ، /production/employees ، /production/shifts
- مسیرهای ماژول تولید اکنون فقط برای نقش admin قابل دسترسی هستند.
- اتصال ماشین تولید به دستگاه CMMS با بررسی وجود dastgahjadid.cod انجام می‌شود و رکورد CMMS تغییر نمی‌کند.
- اجرای واقعی روی سرور هنوز انجام نشده است.


## پیشرفت جدید — 2026-09-25
- رابطه محصول/ایستگاه تولید پیاده‌سازی شد.
- مسیر جدید: /production/station-products
- برای هر رابطه می‌توان زمان سیکل، استاندارد تولید ساعتی و حد مجاز ضایعات را ثبت کرد.
- فعلاً این مقادیر فقط Master Data هستند و در OEE/تشویقی محاسبه نمی‌شوند.


## پیشرفت جدید — برنامه‌ریزی تولید — 2026-09-25
- مسیر /production/plans برای ایجاد و مشاهده برنامه‌های تولید اضافه شد.
- برنامه ابتدا با وضعیت draft ساخته می‌شود و created_by از کاربر واردشده ثبت می‌گردد.
- مسیر /production/plans/<id> برای افزودن آیتم برنامه اضافه شد.
- هر آیتم شامل محصول، ایستگاه اختیاری، مقدار هدف، هدف مدیریتی، روز کاری و توضیحات است.
- اگر ایستگاه انتخاب شود، وجود رابطه فعال محصول/ایستگاه قبل از ثبت آیتم کنترل می‌شود.
- برنامه بدون آیتم قابل تأیید نیست.
- مسیر /production/plans/<id>/approve برای تأیید برنامه اضافه شد؛ پس از تأیید وضعیت approved شده و افزودن آیتم مسدود می‌شود.
- لینک برنامه‌های تولید به داشبورد ماژول تولید اضافه شد.
- ثبت واقعی تولید، توقف، ضایعات، OEE و تشویقی هنوز پیاده‌سازی نشده و قرار نیست قبل از تثبیت قواعد Excel انجام شود.
- اجرای عملی روی سرور هنوز انجام نشده است.

## آخرین پیشرفت — ثبت واقعی تولید
- جدول `production_entries` به `database.py` اضافه شد.
- مسیر `/production/entries` در `production.py` اضافه شد و فعلاً فقط برای admin فعال است.
- ثبت تولید فقط به آیتم‌های برنامه با وضعیت `approved` اجازه می‌دهد.
- هر رکورد به برنامه، شیفت و پرسنل متصل است و اتصال ماشین به ایستگاه برنامه نیز کنترل می‌شود.
- مقدار تولید باید عدد محدود (finite) و بیشتر از صفر باشد.
- صفحه `templates/production_entries.html` ساخته و به داشبورد تولید متصل شد.
- ضایعات، دوباره‌کاری، توقف و OEE هنوز عمداً جدا و پیاده‌سازی نشده‌اند تا مدل رویدادها مطابق Excel تثبیت شود.
- یک خطای تکراری در مسیر تأیید برنامه که UPDATE را دو بار اجرا می‌کرد نیز در همین مرحله اصلاح شد.
- تست اجرای واقعی روی Windows Server هنوز انجام نشده است.

### تکمیل ثبت توقف تولید — 2026-09-25
- مسیر `/production/stops` در `production.py` اضافه شد.
- ثبت توقف شامل شیفت، نوع توقف، تاریخ، زمان شروع و پایان و مدت محاسبه‌شده است.
- اتصال اختیاری به آیتم برنامه، پرسنل و ماشین پشتیبانی می‌شود.
- برای آیتم برنامه فقط برنامه‌های `approved` مجاز هستند.
- فعال‌بودن شیفت، نوع توقف، پرسنل و ماشین کنترل می‌شود و تطابق ماشین با ایستگاه آیتم برنامه نیز بررسی می‌شود.
- صفحه `templates/production_stops.html` برای نمایش و ثبت رویداد توقف استفاده می‌شود.
- لینک «ثبت توقف تولید» به داشبورد اضافه شد.
- تست روی Windows Server هنوز انجام نشده است.

### قدم بعدی
- اضافه‌کردن رویدادهای توقف تولید و سپس مدل ضایعات/دوباره‌کاری، با حفظ جداسازی از ثبت تولید اصلی.
- مدل پایه انواع توقف تولید در `production_stop_types` اضافه شد؛ شامل کد، نام، دسته و مشخص‌کردن اثر آن بر عدم‌دسترسی OEE.
- صفحه مدیریت انواع توقف در `/production/stop-types` اضافه شد.
- جدول رویدادهای توقف `production_stops` نیز به schema اضافه شده و برای مرحله ثبت توقف آماده است؛ route ثبت رویداد توقف هنوز باید در قدم بعدی تکمیل و تست شود.
\n\n### پیشرفت جدید — ضایعات و دوباره‌کاری — 2026-09-25
- جدول `production_defect_types` برای انواع عیب ایجاد شد.
- جدول `production_waste_entries` برای ثبت رویدادهای ضایعات و دوباره‌کاری ایجاد شد.
- نوع رویداد به‌صورت صریح `waste` یا `rework` ذخیره می‌شود؛ این با تحلیل Excel که نوع 1 را ضایعات و نوع 2 را دوباره‌کاری نشان می‌دهد سازگار است، اما فرمول‌های OEE هنوز اعمال نشده‌اند.
- مسیر `/production/defect-types` برای مدیریت انواع عیب اضافه شد.
- مسیر `/production/waste` برای ثبت ضایعات/دوباره‌کاری اضافه شد.
- مقدار رویداد باید مثبت و معتبر باشد.
- اتصال اختیاری به برنامه، پرسنل و ماشین پشتیبانی شده و تطابق ماشین با ایستگاه برنامه کنترل می‌شود.
- صفحه‌ها و لینک‌های داشبورد اضافه شدند.
- طبقه‌بندی‌هایی مثل «کپه/دسته/کامل» فعلاً به‌صورت فیلد متنی نگهداری می‌شوند تا قبل از تثبیت کامل قواعد Excel، مدل بیش از حد سخت‌گیرانه نشود.
- OEE، حد مجاز ضایعات، فرمول تشویقی و گزارش‌های نهایی هنوز عمداً محاسبه نمی‌شوند.
- تست عملی روی Windows Server هنوز انجام نشده است.


### پیشرفت جدید — کنترل و گزارش تجمیعی تولید — 2026-09-25
- مسیر `/production/control` برای کنترل داده‌های تولید اضافه شد.
- گزارش بر اساس آیتم‌های برنامه تأییدشده، تولید واقعی، ضایعات و دوباره‌کاری را کنار هم نشان می‌دهد.
- فیلترهای تاریخ، محصول، ایستگاه، شیفت و پرسنل اضافه شده‌اند.
- برای هر آیتم، هدف برنامه، تولید واقعی، انحراف تولید، مقدار ضایعات، دوباره‌کاری، درصد ضایعات واقعی و حد مجاز ضایعات نمایش داده می‌شود.
- حد مجاز مقداری فعلاً از فرمول شفاف «تولید واقعی × درصد مجاز» محاسبه می‌شود و مازاد ضایعات نیز جداگانه نمایش داده می‌شود.
- این گزارش عمداً OEE و تشویقی را محاسبه نمی‌کند تا تعریف نهایی قواعد Excel تثبیت شود.
- لینک «کنترل تولید» به داشبورد ماژول تولید اضافه شد.
- هنوز تست عملی این گزارش روی Windows Server انجام نشده و باید پس از استقرار کد انجام شود.
### تصمیم مرحله بعد — کنترل یکپارچه توقف — 2026-09-25
- گزارش کنترل تولید اکنون علاوه بر برنامه/تولید/ضایعات/دوباره‌کاری، مجموع زمان توقف و زمان توقف‌هایی که نوع آن‌ها `counts_as_unavailability=1` است را نیز نشان می‌دهد.
- این مرحله هنوز OEE را محاسبه نمی‌کند؛ داده توقف صرفاً به‌صورت خام و تفکیک‌شده برای آماده‌سازی مرحله بعد نگهداری می‌شود.
- قدم بعدی پس از تست عملی: بررسی صحت داده‌ها روی سرور و سپس نهایی‌سازی تعاریف زمان برنامه‌ریزی‌شده، توقف قابل محاسبه و فرمول‌های OEE بر اساس قواعد Excel.


### پیشرفت جدید — حضور و وضعیت پرسنل تولید — 2026-09-26
- جدول `production_attendance` برای ثبت وضعیت پرسنل به تفکیک تاریخ کاری، شیفت و پرسنل اضافه شد.
- وضعیت‌های پایه فعلاً شامل حاضر، غایب، مرخصی و تعطیل/استراحت هستند.
- شروع و پایان حضور به‌صورت اختیاری ذخیره می‌شود و رکورد تکراری برای یک پرسنل در یک تاریخ کاری و شیفت مجاز نیست.
- مسیر `/production/attendance` برای ثبت و فیلتر رکوردها اضافه شد و به داشبورد تولید متصل شد.
- این مرحله عمداً زمان مفید، اضافه‌کاری، زمان برنامه‌ریزی‌شده و OEE را محاسبه نمی‌کند؛ این تعاریف باید بعد از تطبیق با Excel تثبیت شوند.

### پیشرفت جدید — وقفه‌های پرسنلی — 2026-09-26
- `production_employee_break_types` برای تعریف انواع وقفه پرسنلی اضافه شد؛ دسته‌ها: استراحت، شخصی، مرخصی، سایر.
- هر نوع وقفه یک پرچم مستقل `counts_as_unavailability` دارد تا بعداً بدون قاطی‌کردن با توقف دستگاه در محاسبات دسترسی استفاده شود.
- `production_employee_breaks` برای ثبت شروع/پایان و مدت وقفه بر اساس رکورد حضور اضافه شد.
- ثبت وقفه خارج از بازه حضور همان رکورد مجاز نیست.
- گزارش فعلی هنوز این وقفه‌ها را وارد OEE یا کنترل تولید نمی‌کند؛ این جداسازی عمدی است.


### پیشرفت جدید — تقویم کاری و زمان برنامه‌ریزی‌شده — 2026-09-26
- جدول `production_work_calendar` برای ثبت زمان برنامه‌ریزی‌شده به تفکیک تاریخ کاری، شیفت و ایستگاه اضافه شد.
- وضعیت کاری/غیرکاری و زمان برنامه‌ریزی‌شده (دقیقه) جداگانه ذخیره می‌شود؛ روز غیرکاری می‌تواند زمان برنامه‌ریزی‌شده صفر داشته باشد.
- برای هر ترکیب تاریخ + شیفت + ایستگاه فقط یک رکورد مجاز است و ثبت مجدد همان ترکیب، رکورد قبلی را به‌روزرسانی می‌کند.
- اگر زمان برنامه‌ریزی‌شده خالی باشد، مدت شیفت از ساعت شروع/پایان شیفت به‌صورت خودکار محاسبه می‌شود.
- مسیر `/production/work-calendar` و صفحه مربوط به آن اضافه شد و به داشبورد متصل شد.
- این جدول عمداً از حضور پرسنل و توقف دستگاه جداست و هنوز وارد محاسبات OEE نشده است.
- اجرای عملی روی Windows Server هنوز انجام نشده است.


### پیشرفت جدید — گزارش زمان برنامه‌ریزی‌شده — 2026-09-26
- مسیر `/production/planned-time-report` اضافه شد.
- گزارش به تفکیک تاریخ + شیفت + ایستگاه، زمان برنامه‌ریزی‌شده، توقف، عدم‌دسترسی ثبت‌شده، تولید واقعی، ضایعات و دوباره‌کاری را کنار هم نمایش می‌دهد.
- گزارش فقط از روزها/شیفت‌ها/ایستگاه‌هایی استفاده می‌کند که در `production_work_calendar` ثبت شده‌اند؛ بنابراین نبودن رکورد تقویم به‌صورت ضمنی به معنی صفر یا روز کاری نیست.
- Availability و OEE عمداً در این گزارش محاسبه نشده‌اند تا تعریف نهایی زمان برنامه‌ریزی‌شده و قواعد کسر توقف‌ها قبل از محاسبه تثبیت شود.
- لینک گزارش به داشبورد تولید اضافه شد.


### پیشرفت جدید — طبقه‌بندی توقف‌های برنامه‌ریزی‌شده — 2026-09-26
- به `production_stop_types` فیلد `is_planned_stop` اضافه شد.
- `counts_as_unavailability` و `is_planned_stop` دو مفهوم مستقل هستند: اولی مشخص می‌کند توقف در زمان عدم‌دسترسی ثبت‌شده لحاظ شود؛ دومی مشخص می‌کند توقف از زمان برنامه‌ریزی‌شده کسرشدنی/برنامه‌ریزی‌شده است.
- برای دیتابیس‌های قبلی، migration خودکار ستون `is_planned_stop` اضافه شد.
- فرم مدیریت انواع توقف اکنون هر دو ویژگی را جداگانه ثبت و نمایش می‌دهد.
- گزارش زمان برنامه‌ریزی‌شده اکنون زمان برنامه‌ریزی‌شده، توقف برنامه‌ریزی‌شده، زمان برنامه‌ریزی خالص، توقف کل و عدم‌دسترسی ثبت‌شده را جداگانه نشان می‌دهد.
- زمان برنامه‌ریزی خالص فعلاً فقط به‌صورت `max(planned_minutes - planned_stop_minutes, 0)` گزارش می‌شود؛ Availability و OEE هنوز محاسبه نمی‌شوند.
- این تفکیک برای جلوگیری از مخلوط‌شدن توقف برنامه‌ریزی‌شده با خرابی/عدم‌دسترسی در مرحله بعدی OEE انجام شد.


### پیشرفت جدید — کنترل بازه و هم‌پوشانی توقف‌ها — 2026-09-26
- ثبت توقف اکنون زمان شروع و پایان را با بازه واقعی شیفت انتخاب‌شده تطبیق می‌دهد؛ توقف خارج از شیفت رد می‌شود.
- برای جلوگیری از دوباره‌شماری زمان، توقف‌های هم‌پوشان در یک شیفت برای همان ماشین، یا برای رکوردهای بدون ماشین و همان آیتم/زمینه، رد می‌شوند.
- این کنترل قبل از محاسبات آینده Availability/OEE اضافه شد تا مجموع دقیقه‌های توقف قابل اتکا باشد.
- اصلاح نهایی پس از بازبینی انجام شد: کنترل هم‌پوشانی بعد از تعیین `plan_item` اجرا می‌شود.


### پیشرفت جدید — زمان توقف overlap-safe و برش به پنجره برنامه — 2026-09-26
- گزارش `/production/planned-time-report` اکنون توقف‌ها را فقط در پنجره برنامه‌ریزی‌شده همان تاریخ/شیفت/ایستگاه محاسبه می‌کند.
- اگر `planned_minutes` کمتر از طول شیفت باشد، پنجره از شروع شیفت تا همان تعداد دقیقه در نظر گرفته می‌شود؛ توقف خارج از این پنجره وارد محاسبه نمی‌شود.
- مجموع توقف‌ها با Union بازه‌ها محاسبه می‌شود تا هم‌پوشانی‌ها دوباره‌شماری نشوند.
- `planned_stop_minutes` فقط توقف‌های `is_planned_stop=1` را شامل می‌شود.
- `unavailability_minutes` در این گزارش فقط توقف‌های `counts_as_unavailability=1` و `is_planned_stop=0` را شامل می‌شود؛ بنابراین توقف برنامه‌ریزی‌شده به‌اشتباه به‌عنوان عدم‌دسترسی غیر برنامه‌ریزی‌شده وارد Availability آینده نمی‌شود.
- `net_planned_minutes = planned_minutes - planned_stop_minutes` با کف صفر محاسبه می‌شود.
- Availability و OEE همچنان عمداً محاسبه نمی‌شوند تا تعریف زمان مفید و سایر قواعد نهایی شود.


### پیشرفت جدید — زمان قابل‌استفاده برای مبنای Availability — 2026-09-26
- گزارش زمان برنامه‌ریزی‌شده اکنون `available_minutes` را نیز محاسبه می‌کند.
- فرمول فعلی: `available_minutes = max(planned_minutes - planned_stop_minutes - unavailability_minutes, 0)`.
- این مقدار «زمان قابل‌استفاده تولید» است و هنوز نسبت Availability یا OEE محاسبه نمی‌شود.
- توقف برنامه‌ریزی‌شده ابتدا از زمان برنامه کسر می‌شود و سپس عدم‌دسترسی غیر برنامه‌ریزی‌شده از زمان باقی‌مانده کسر می‌شود.
- این تفکیک مبنای مرحله بعدی تعریف رسمی Availability خواهد بود.


### پیشرفت جدید — تعریف رسمی Availability — 2026-09-26
- Availability در گزارش `/production/planned-time-report` اکنون بر مبنای زمان برنامه‌ریزی خالص محاسبه می‌شود.
- فرمول رسمی فعلی: `Availability % = available_minutes / net_planned_minutes × 100`.
- `net_planned_minutes = max(planned_minutes - planned_stop_minutes, 0)`.
- `available_minutes = max(net_planned_minutes - unavailability_minutes, 0)`.
- مخرج Availability برای روز کاری، زمان برنامه‌ریزی خالص است؛ اگر این مقدار صفر باشد Availability به‌صورت «—» نمایش داده می‌شود و صفرِ مصنوعی تولید نمی‌شود.
- برای روزهای غیرکاری، حتی اگر داده قدیمی `planned_minutes` داشته باشد، زمان برنامه‌ریزی و Availability برابر صفر/غیرقابل‌محاسبه در نظر گرفته می‌شود.
- Availability کل گزارش به‌صورت weighted calculation از مجموع `available_minutes / net_planned_minutes` محاسبه می‌شود تا یک روز کوتاه یا بلند وزن مساوی اشتباه نگیرد.
- نسخه تکراری و بلااستفاده تابع `planned_time_report` از انتهای `production.py` حذف شد تا فقط یک پیاده‌سازی مرجع برای این route باقی بماند.
- OEE هنوز عمداً محاسبه نمی‌شود؛ مرحله بعد باید قبل از پیاده‌سازی OEE، تعریف Performance، Quality و رفتار آنها در حالت‌های بدون تولید/بدون Cycle Time را تثبیت کند.


### پیشرفت جدید — تثبیت تعریف Quality / Performance / OEE — 2026-09-26
- فایل مرجع جدید `AI_CONTEXT/PRODUCTION_OEE_DEFINITIONS.md` ایجاد شد.
- Quality فعلاً بر اساس `(production - waste - rework) / production` تعریف شده و در تولید صفر، قابل محاسبه نیست.
- Performance فقط با Cycle Time استاندارد معتبر محاسبه خواهد شد؛ اولویت Cycle Time برای ترکیب محصول+ایستگاه و سپس مقدار پیش‌فرض ایستگاه است.
- Performance سقف 100٪ دارد و در نبود Available Minutes یا Cycle Time معتبر، قابل محاسبه نیست.
- OEE فقط وقتی نمایش داده می‌شود که Availability، Performance و Quality هر سه قابل محاسبه باشند.
- برای تجمیع چند محصول/ایستگاه، یک Cycle Time دلخواه برای کل مجموعه انتخاب نمی‌شود؛ ابتدا باید در سطح محصول+ایستگاه محاسبه شود.
- Rework هنوز از نظر فرآیند کسب‌وکار باید با نحوه ثبت تولید واقعی کنترل شود؛ تعریف فعلی برای گزارش است و داده موجود را تغییر نمی‌دهد.
- Employee Break عمداً بخشی از Availability ماشین/ایستگاه نیست.
- گام بعدی مناسب: تست محاسبات با داده‌های مصنوعی/واحد و سپس ساخت گزارش Performance/Quality و در نهایت OEE.


### پیشرفت جدید — تست واحد شاخص‌های OEE — 2026-09-26
- فایل `tests/test_production_oee_metrics.py` اضافه شد.
- تست‌های Quality: محاسبه عادی، کف صفر، تولید صفر، داده غیرمتناهی.
- تست‌های Performance: محاسبه عادی، سقف 100٪، Cycle Time/Available Time نامعتبر و داده غیرمتناهی.
- تست‌های OEE: الزام وجود هر سه مؤلفه، محاسبه پایه و سقف 100٪.
- هدف این تست‌ها این است که قبل از اتصال شاخص‌ها به گزارش OEE، فرمول‌های پایه در برابر حالت‌های مرزی تثبیت شوند.
- اجرای محیط واقعی تست از طریق GitHub انجام نشد؛ بنابراین این commit فقط تست‌ها را اضافه می‌کند و ادعای اجرای موفق pytest روی محیط کاربر ندارد.


### پیشرفت جدید — گزارش Performance و Quality — 2026-09-26
- route جدید `/production/performance-quality` اضافه شد.
- گزارش در سطح محصول + ایستگاه + تاریخ + شیفت ساخته شده تا Cycle Time بین محصولات مخلوط نشود.
- Quality بر اساس تولید، ضایعات و دوباره‌کاری محاسبه می‌شود.
- Performance فقط با Cycle Time معتبر محاسبه می‌شود و سقف 100٪ دارد.
- Cycle Time ابتدا از `production_station_products.standard_cycle_time_seconds` و در صورت معتبر نبودن از `production_stations.cycle_time_seconds` گرفته می‌شود.
- زمان Available از همان تعریف رسمی Availability و تقویم کاری استفاده می‌کند.
- تجمیع تولید/ضایعات برای چند plan item هم‌محصول/هم‌ایستگاه اصلاح شد تا رویدادها چندبار شمرده نشوند.
- گزارش و لینک آن به داشبورد تولید اضافه شد.
- OEE هنوز در این مرحله به‌صورت گزارش نهایی فعال نشده؛ ابتدا باید همین گزارش روی داده واقعی بررسی شود و سپس OEE نهایی روی آن سوار شود.


### پیشرفت جدید — گزارش OEE — 2026-09-26
- route جدید `/production/oee` اضافه شد.
- OEE در سطح محصول + ایستگاه + شیفت محاسبه می‌شود.
- Availability از تقویم کاری و توقف‌های طبقه‌بندی‌شده استفاده می‌کند.
- Performance از Cycle Time محصول/ایستگاه و در صورت نبود آن از Cycle Time پیش‌فرض ایستگاه استفاده می‌کند.
- Quality از تولید، ضایعات و دوباره‌کاری محاسبه می‌شود.
- OEE فقط وقتی مقدار دارد که هر سه مؤلفه Availability/Performance/Quality قابل محاسبه باشند.
- OEE کل گزارش عمداً نمایش داده نمی‌شود؛ چون تجمیع Performance بین Cycle Timeهای متفاوت بدون روش تجمیع رسمی معتبر نیست.
- لینک گزارش OEE به داشبورد تولید اضافه شد.
- این مرحله OEE را فعال کرده، اما قبل از هر تغییر بیشتر باید روی داده واقعی و سناریوهای واقعی تولید بررسی شود.

### بازبینی فنی OEE — 2026-09-26
- یک ریسک مهم در OEE شناسایی و اصلاح شد: وقتی در یک ترکیب تاریخ + شیفت + ایستگاه چند محصول برنامه‌ریزی شده باشد، زمان Available متعلق به ایستگاه/شیفت مشترک است و نباید بدون قاعده تخصیص به هر محصول نسبت داده شود.
- در این حالت، Quality و Availability ردیف‌ها همچنان گزارش می‌شوند، اما Performance و OEE عمداً «—» می‌شوند و علت در UI مشخص است.
- این رفتار از تولید OEE مصنوعی/دو بار تخصیص زمان جلوگیری می‌کند تا روش رسمی تخصیص زمان بین محصولات بر اساس داده واقعی کسب‌وکار مشخص شود.
- گزارش OEE همچنان در سطح محصول + ایستگاه + شیفت باقی می‌ماند.
- مرحله بعدی: ادامه ممیزی فنی OEE، به‌خصوص رفتار توقف ماشین‌های متعدد در یک ایستگاه و سپس طراحی داشبورد مدیریتی OEE بدون ایجاد تجمیع ریاضی نامعتبر.


### ممیزی چندماشینه OEE — 2026-09-26
- بررسی شد که توقف ماشین‌محور در ایستگاه چندماشینه فعلاً با مدل داده موجود قابل تفسیر دقیق نیست؛ سیستم هنوز ظرفیت موازی، گلوگاه و امکان ادامه تولید با ماشین دیگر را مدل نمی‌کند.
- بنابراین رفتار فعلی به‌صورت محافظه‌کارانه حفظ و مستندسازی شد: توقف هر ماشین متعلق به ایستگاه در Availability ایستگاه لحاظ می‌شود.
- این تصمیم به‌عنوان محدودیت مدل ثبت شد و از اعمال یک فرض پنهان درباره ظرفیت موازی جلوگیری می‌کند.
- برای OEE دقیق ماشین/ایستگاه در آینده، مدل ظرفیت/Topology باید قبل از تغییر فرمول Availability اضافه شود.


### داشبورد مدیریتی OEE — 2026-09-26
- کارت‌های مدیریتی گزارش OEE تکمیل شد: زمان برنامه خالص، زمان قابل‌استفاده، Availability وزنی، تولید کل، Quality وزنی، تعداد ردیف OEE معتبر، ردیف‌های چندمحصولی و ردیف‌های بدون Cycle Time معتبر.
- شمارنده‌های کیفیت محاسبه به route اضافه شدند تا مدیر بتواند تفاوت «نبود داده کافی» و «OEE معتبر» را ببیند.
- OEE کل همچنان عمداً نمایش داده نمی‌شود؛ Performance/OEE بین Cycle Timeهای متفاوت بدون روش تجمیع رسمی معتبر نیست.


### ممیزی ورودی‌های OEE — 2026-09-26
- Quality اکنون مقادیر منفی Production/Waste/Rework را معتبر نمی‌داند و مقدار نامعتبر برمی‌گرداند؛ از پنهان شدن داده خراب پشت سقف 100٪ جلوگیری شد.
- تست‌های واحد برای مقادیر منفی اضافه شدند.
- تست‌های موجود را در محیط واقعی کاربر اجرا نکرده‌ایم؛ صرفاً پوشش تستی در repository به‌روزرسانی شده است.


### ممیزی دوباره‌شماری رویدادهای OEE — 2026-09-26
- تجمیع تولید و ضایعات/دوباره‌کاری در گزارش OEE بر اساس plan_item_id به‌صورت مرحله‌ای انجام می‌شود.
- این ساختار تضمین می‌کند اگر یک محصول برای یک روز/ایستگاه در چند Plan Item مجزا وجود داشته باشد، مقدار هر entry فقط یک‌بار وارد مجموع شود و joinهای چندمرحله‌ای باعث تکثیر quantity نشوند.
- رویدادهای بدون plan_item_id در OEE فعلی عمداً وارد محاسبه محصول/ایستگاه نمی‌شوند؛ چون نسبت دادن آن‌ها به محصول یا ایستگاه بدون مرجع معتبر می‌تواند OEE را تحریف کند.


### ممیزی هم‌پوشانی توقف‌ها در Availability — 2026-09-26
- یک ایراد مفهومی در برخورد توقف برنامه‌ریزی‌شده و توقف عدم‌دسترسی شناسایی و اصلاح شد.
- اگر توقف برنامه‌ریزی‌شده و توقف غیر برنامه‌ریزی‌شده روی یک بازه هم‌پوشانی داشته باشند، بخش هم‌پوشان دیگر به‌عنوان عدم‌دسترسی دوباره کسر نمی‌شود.
- برای این کار اتحاد توقف‌های برنامه‌ریزی‌شده ساخته و بازه‌های عدم‌دسترسی از آن کم می‌شوند.
- تست‌های واحد برای حذف هم‌پوشانی و چند blocker اضافه شدند.


### ممیزی تاریخ و شیفت‌های عبوری از نیمه‌شب — 2026-09-26
- helper جدید `_shift_window()` تاریخ شروع شیفت را به‌عنوان anchor رسمی بازه شیفت می‌سازد و برای شیفت‌های عبوری از نیمه‌شب پایان را به روز بعد منتقل می‌کند.
- محاسبه Availability در `_stop_minutes_for_calendar_row` اکنون از همین helper مشترک استفاده می‌کند؛ بنابراین پنجره توقف با همان معنای تاریخ شروع شیفت ساخته می‌شود.
- در ثبت توقف، `production_date` اکنون صراحتاً «تاریخ شروع شیفت» است. برای شیفت شب، توقفی که بعد از نیمه‌شب رخ دهد همچنان با تاریخ شروع شیفت ثبت و گزارش می‌شود.
- اگر توقف به Plan Item متصل باشد، `production_date` باید با `work_day` همان Plan Item برابر باشد.
- در ثبت تولید و ثبت ضایعات/دوباره‌کاری نیز تاریخ رویداد باید با `work_day` آیتم برنامه برابر باشد؛ این کار از شکستن یک شیفت شب بین دو روز تقویمی در گزارش جلوگیری می‌کند.
- توقف‌های داخل پنجره برنامه‌ریزی کوتاه‌تر از کل شیفت فقط در همان پنجره محاسبه می‌شوند و توقف بعد از پنجره وارد Availability نمی‌شود.
- تست‌های واحد برای شیفت عادی، شیفت عبوری از نیمه‌شب، توقف خارج از پنجره برنامه و توقف بعد از نیمه‌شب اضافه شدند.
- اجرای pytest در محیط واقعی کاربر انجام نشده است؛ بنابراین وضعیت تست‌ها «افزوده‌شده، نیازمند اجرا در محیط پروژه» است.

### ممیزی سازگاری Plan Item با رویدادهای تولید — 2026-09-26
- در ثبت توقف و ثبت ضایعات/دوباره‌کاری، Query مربوط به Plan Item از `work_day` استفاده می‌کرد اما آن را در SELECT برنمی‌گرداند؛ این خطا اصلاح شد.
- ثبت تولید، توقف و ضایعات/دوباره‌کاری اکنون همگی می‌توانند قاعده «تاریخ رویداد = work_day آیتم برنامه» را درست اجرا کنند.
- سازگاری ماشین با ایستگاه Plan Item در مسیرهای مربوطه بررسی و حفظ شد.
- فعلاً Shift به Plan Item وابسته نشده و تقویم کاری نیز پیش‌شرط ثبت رویداد نیست؛ چون مدل فعلی چنین رابطه‌ای تعریف نکرده است.
- commit: 691f381e2329c9b59d10a3dc921aac06eea28335
